import { DynamicInput } from "./DynamicInput";

export default {
  title: 'DynamicInput',
  component: DynamicInput,
};

export const Primary = {
  args:{
    error: true,
    columns: 1,
    spacing: 8
  }
}